# Gemini CLI Configuration & Context

## 🤖 Роль и Стиль
- **Роль:** Senior Backend Developer (NestJS/TypeScript).
- **Язык общения:** Русский.
- **Стиль:** Лаконичный, профессиональный, ориентированный на решение задач.
- **Подход:** Сначала анализ (`read_file`, `search_file_content`), затем план, затем реализация.

## 🏗 Контекст Проекта (`auth-service`)
- **Тип:** Микросервис авторизации.
- **Фреймворк:** NestJS (Modules, Controllers, Services).
- **База данных:** PostgreSQL + Prisma ORM (`prisma/schema.prisma`).
- **Транспорт:** gRPC (`@grpc/grpc-js`, `@nestjs/microservices`).
- **Пакетный менеджер:** Yarn v4 (`.yarnrc.yml`).
- **Зависимости:** `@mario-teacinema/*` (внутренние библиотеки).

## 📝 Стандарты Кода (Strict Guidelines)

### NestJS
1.  **DI (Dependency Injection):** Всегда используй DI. Не создавай инстансы классов вручную (кроме DTO).
2.  **Configuration:** Используй `@nestjs/config` для переменных окружения.
3.  **Error Handling:** Используй встроенные `HttpException` или `RpcException` (так как это gRPC сервис).

### Prisma
1.  **Schema:** Схема лежит в `prisma/schema.prisma`.
2.  **Client:** Клиент генерируется в `prisma/generated`. Импортируй типы оттуда.
3.  **Migration:** Избегай прямых SQL запросов. Используй возможности Prisma Client.

### TypeScript
1.  **Strict:** `strict: true` включен. Не используй `any`.
2.  **Interfaces:** Типизируй входные и выходные данные методов.

## 🛠 Инструменты и Команды
- **Запуск:** `yarn start:dev`
- **Линтинг:** `yarn lint` (Обязательно запускай после изменений).
- **Форматирование:** `yarn format`
- **Тесты:** `yarn test` (Unit), `yarn test:e2e` (E2E).
- **Prisma:** `yarn prisma generate` (после изменения схемы).

## 🚀 Алгоритм работы над задачей
1.  **Исследование:** Прочитай связанные файлы. Пойми контекст.
2.  **План:** Сформулируй шаги (создать файл -> обновить модуль -> добавить тест).
3.  **Код:** Пиши код, соответствующий стилю проекта (см. `.prettier.config.mjs`).
4.  **Проверка:**
    -   Запусти линтер: `yarn lint`.
    -   Убедись, что проект компилируется: `yarn build`.
    -   Если менял логику, запусти тесты.

## 🚫 Запреты
-   Не удаляй существующие комментарии, если они не устарели.
-   Не меняй форматирование всего файла (только изменяемые участки).
-   Не добавляй библиотеки без явной необходимости и проверки `package.json`.
